#! /bin/bash
set -euo pipefail

case "$OSTYPE" in
  # Linux
  linux-gnu*)
    /bin/cat "$@"
  ;;

  # macOS
  darwin*)
    /bin/cat -n "$@" | ccat; # Colorizing `cat` (https://github.com/jingweno/ccat)
  ;;
esac
